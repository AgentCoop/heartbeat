#!/usr/bin/env bash

set -e

SRC="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
PKGROOT=$(realpath $SRC/..)

protoc \
    -I$PKGROOT/api \
    --go_out=$PKGROOT/internal \
    --go-grpc_out=$PKGROOT/internal \
    $PKGROOT/api/heartbeat.proto

#!/usr/bin/env bash

set -e

SRC="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
PKGROOT=$(realpath $SRC/..)

(
  cd $PKGROOT/src/heartbeat.service;
  env GOPATH=$PKGROOT go "$@"
)
